const coordinates = {
    x: 100,
    y: 200
};
const coordinates2 = {
    x: 300,
    y: 400
};

const { x: x, y: y } = coordinates;
const { x: x2, y: y2 } = coordinates2;
// --SNIP--

export default { x, y, x2, y2 };
